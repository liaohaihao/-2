pluginManagement {
    repositories {
        // Standard repositories for Android + Gradle plugins
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "SuZaiFlowerSystem"
include(":app")
 